# CustomerScripts

ElarVersionWrapper: A lightweight PowerShell toolset that enforces dynamic expiration dates (soft-expiration / kill-switch) for legacy Windows applications.
It replaces local binaries with a transparent wrapper to block outdated clients after backend updates until automated software deployment completes
